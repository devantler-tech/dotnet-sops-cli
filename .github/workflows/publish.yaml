name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@0e68692337af526443489c48c5c9b199080f2b8d # v1.21.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
