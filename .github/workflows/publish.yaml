name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@38555ecd85c32dcd593509e3aef4dddd206c1980 # v1.15.4
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
