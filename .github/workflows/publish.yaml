name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@e5c7943bc0eaea81d859415319d9f5c17d1470e9 # v1.6.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
