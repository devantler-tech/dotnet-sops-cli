name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@3dac098623090e99c3de0b356b67626153acd4a5 # v1.19.4
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
