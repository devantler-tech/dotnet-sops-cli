name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@ce6415046b541e5f000a3a6c3390916b6abf4e30 # v1.2.4
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
