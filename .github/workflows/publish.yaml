name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@c724f206eb0d77ddb87e0dd66f2bbc2c68ec3327 # v1.12.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
